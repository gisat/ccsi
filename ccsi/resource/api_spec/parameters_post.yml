Register resource parameters set container
---
tags:
  - parameters
produces:
  - application/json
parameters:
  - in: resource_name
    name: resource name
    type: string
    required: true
responses:
  200:
    description: Container for resourse praremeters for given resource is created
